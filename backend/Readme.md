# ServiceDesk Pro — Backend (MVP)

Run:
1. `cp .env.example .env` and fill in `MONGO_URI` / `JWT_SECRET`.
2. `npm install`
3. `npm run dev` (auto-seeds demo data on first run if `SEED_ON_START=true`)

See root `PLAN.md` for the full phased roadmap.
