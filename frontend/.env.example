# No VITE_API_URL needed: the frontend calls "/api" and the Vite dev
# proxy (vite.config.js) or the Vercel rewrite (vercel.json) forward it
# to the backend. This file exists so it isn't mistaken for missing config.
